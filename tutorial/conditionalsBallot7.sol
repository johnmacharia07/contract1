// import solidity
pragma solidity ^0.8.14;

contract Simple {
    //if statement
    if (a == 2) {
        // expression/code  if condition met
    } else {
        // expression/code if condition met
    }

    //checking condition first
    while (a >= 0) {
        // expression/code if condition met
    }

    /* do run this code until condition no
    longer true */
    do {
        // do this code first
    } while (a >= 0)

    // for loop
    for (uint i=0; i<=50; i++) {
        // code
    }
}